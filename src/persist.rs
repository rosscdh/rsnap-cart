use bson::Bson;


use std::str;

#[derive(Clone)]
pub struct Persist;

impl Persist {
    fn detail_cart(cart_id: &str) -> () {
    }

    fn modify_cart(cart_id: &str) -> () {
    }

    fn remove_cart(cart_id: &str) -> () {
    }

    fn add_product(cart_id: &str) -> () {
    }
}
